# Fictional guild demo

Run `PYTHONPATH=src uv run --no-sync python -m gargantua_showcase.demo_data`
from the repository root after the documented dependency sync. The command recreates
synthetic_data/guild.json. It contains two fictional members and no message
content, ticket, sanction or external identifier.
