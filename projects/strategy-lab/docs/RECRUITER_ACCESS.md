# Recruiter access

A fifteen-minute review can start with protocol.py and its two failure tests,
then statistics.py and llm_contract.py. I can explain why the holdout is
consumed once, why executability precedes a performance claim and why invalid
LLM output abstains.

A broader sanitized review repository may be provided after an initial
interview for 7 to 14 days, read-only and without data, execution credentials
or deployment permissions. Read-only does not prevent copying.
